# Gap Drills

Open gaps surfaced during review. Muscle = cold-write drill. Conceptual = up to 2 fresh problems.
Purge Closed rows older than 7 days.

| Pattern | Type | Drill | Problem Link | Status |
|---|---|---|---|---|
| monotonic-stack | Muscle | Pop operator RECOVERED (stated `<=` correctly 2026-06-27). Remaining: cold-write the back-to-front method WITHOUT the distance slip — `result[i] = stack.peek() - i`, not `peek()` — and justify on `[73,74]` why `>=` is backwards. | https://leetcode.com/problems/daily-temperatures/ | Closed 2026-07-06 (sprint review: code cold 5/5 incl. [73,74] + plateau; distance line exact; both-direction WHY in own words) |
| simulation-stack / builder-expansion | Muscle | FIRST-TRY complexity for Decode String: state time AND space cold, one shot. Must include: both are output-sensitive Θ(D), input-n is the wrong variable; why a `maxK×` factor over-counts (cost = characters EMITTED — k is already inside the emitted length); bonus: the k=1 nesting re-copy caveat. | https://leetcode.com/problems/decode-string/ | Closed 2026-07-07 (Snippet review: code cold; time AND space output-sensitive first-try in own words — "appends sum exactly to final output length, k×m chunk IS a segment of the output"; maxK× framing gone) |
| sliding-window / variable-shrink | Muscle | Failure-direction drill: cold-run naive `left = lastSeen[c]+1` (no max) on "abba" index-by-index, ONE pass — name the exact index where left moves backward (i=3, second 'a'), write the 3-char window "bba", and state the direction: window too BIG → OVERcounts (never "undercounts"). Then state in one sentence why Math.max fixes it. | https://leetcode.com/problems/longest-substring-without-repeating-characters/ | Open (added 2026-07-06 — redo passed but analysis took 3 passes) |
| sliding-window / max-frequency-window | Conceptual | Record-mechanism WHY, cold-written in ONE sentence: res only hits a new high when maxCount hits a new REAL high (the only raising line is freq[current] just topping it), and at that instant the window is provably valid — stale windows coast but never set records. ⚠️ Reclassified class-B 2026-07-12: this drill REPLACES the redo — run it today; the card's 07-14 review re-tests it. | https://leetcode.com/problems/longest-repeating-character-replacement/ | Open — DUE 2026-07-12, class-B remediation (added 2026-07-06 — 2nd miss on same sentence) |
| binary-search / boundary-template | Muscle | **The `< vs <=` wound — 2nd miss in one week (Koko 07-12 shipped wrong output on this cell; FindMin 07-16 couldn't say why).** Cold-derive, no peeking, in ONE pass: take `while (left <= right)` in the FindMin template and trace what happens at `left == right` — write the values of mid, the comparison result, and the assignment, then name the failure MODE in one word. It is not "off-by-one" and not "slower" — it is an INFINITE LOOP (mid==left==right → nums[mid]<=nums[right] trivially true → right=mid is a no-op → nothing moves). Then state the rule in one sentence: `hi = mid` (keep-branch) REQUIRES `lo < hi`, because `hi = mid` cannot shrink a 1-cell window; the `lo <= hi` form requires `hi = mid - 1`. Cross them and you get Koko. | https://leetcode.com/problems/find-minimum-in-rotated-sorted-array/ | Open — DUE 2026-07-17, class-B remediation (added 2026-07-16 — HIGHEST PRIORITY: repeat gap) |
| binary-search / rotated-array-find-pivot | Conceptual | Decisiveness WHY, cold-written in TWO sentences: (1) Name the family that breaks the LEFT-comparison — the UNROTATED array: `[1,2,3]` → `nums[mid]=2 >= nums[left]=1` says "go right" but the min was at index 0; the signal is AMBIGUOUS (identical when unrotated with min at left, and when the pivot is right of mid) → same signal, opposite correct actions → cannot drive a decision. (2) `nums[mid] <= nums[right]` has ONE meaning only — `[mid..right]` contains no cliff — which PROVES the min is at mid or left of it. Ambiguous vs decisive is the whole reason for the choice. | https://leetcode.com/problems/find-minimum-in-rotated-sorted-array/ | Open — DUE 2026-07-17, class-B remediation (added 2026-07-16 — intuition present, breaking family not named) |
| binary-search / 2d-matrix-flatten | Conceptual | Validity WHY, cold-written in TWO sentences: (1) binary search needs ONE sorted sequence; rows-sorted alone leaves descents at row seams ([1,3,5],[2,4,6] → 1,3,5,2,4,6 — code returns false for target=2 though present). (2) Property 2 (each row's first > previous row's last) welds the seams → flat view globally sorted → discard-half valid. ⚠️ Reclassified class-B 2026-07-12: this drill REPLACES the redo — run it today, and narrate BEFORE coding at the 07-14 card review (the other half of the class-B). | https://leetcode.com/problems/search-a-2d-matrix/ | Open — DUE 2026-07-12, class-B remediation (added 2026-07-07 — WHY not produced in 3 probes) |
